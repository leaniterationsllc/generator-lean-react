import { takeLatest } from 'redux-saga/effects';
import { ACTION } from '../../constants';

export const <%= funcName %> = function* <%= funcName %>(action) {

};

export default takeLatest(ACTION, <%= funcName %>);
