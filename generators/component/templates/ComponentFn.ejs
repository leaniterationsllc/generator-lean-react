import React from 'react';
import PropTypes from 'prop-types';
import { View } from 'react-native';

const <%= name %> = () => (
  <View>
    
  </View>
);

<%= name %>.propTypes = {

};

export default <%= name %>;
